# src/api/simple/routes/ — Route handlers (endpoints) for the simple API.
# Each file defines a group of related endpoints:
#   - recipes.py  → Recipe CRUD and matching queries
#   - inventory.py → Inventory browsing and refresh
#   - matching.py  → Recipe-to-inventory matching detail
#   - ingestion.py → Data ingestion pipeline triggers
